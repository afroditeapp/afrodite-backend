/*
 * pihka-backend
 *
 * Pihka backend API
 *
 * The version of the OpenAPI document: 0.1.0
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// SenderMessageId : Conversation message counter located on the server which only message sender owns (conversation can have 2 senders, so there is two counters).  The server increments the ID automatically when message is sent. The server resets the ID when account interaction is changed to match state. Also the client can reset the counter as it might go out of sync for example when the account is changed to different device.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct SenderMessageId {
    #[serde(rename = "id")]
    pub id: i64,
}

impl SenderMessageId {
    /// Conversation message counter located on the server which only message sender owns (conversation can have 2 senders, so there is two counters).  The server increments the ID automatically when message is sent. The server resets the ID when account interaction is changed to match state. Also the client can reset the counter as it might go out of sync for example when the account is changed to different device.
    pub fn new(id: i64) -> SenderMessageId {
        SenderMessageId {
            id,
        }
    }
}

